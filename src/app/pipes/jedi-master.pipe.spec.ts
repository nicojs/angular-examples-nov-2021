import { JediMasterPipe } from './jedi-master.pipe';

describe('JediMasterPipe', () => {
  it('create an instance', () => {
    const pipe = new JediMasterPipe();
    expect(pipe).toBeTruthy();
  });
});
